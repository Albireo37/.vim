Installation:
git clone https://github.com/Albireo37/.vim.git ~/.vim

Make symlinks:
ln -s ~/.vim/vimrc ~/.vimrc

source:
http://vimcasts.org/episodes/synchronizing-plugins-with-git-submodules-and-pathogen/

Updaten submodules:
cd ~/.vim
git submodule init
git submodule update
